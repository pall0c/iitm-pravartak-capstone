## Getting started

1. Install backend dependencies:

   ```bash
   uv sync
   ```

1. Build the vector store:

   ```bash
   uv run main.py --reset
   ```

1. Start the API:

   ```bash
   uv run fastapi dev
   ```
