[project]
name = "rag"
version = "0.1.0"
description = "iitm - pravartak capstone project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.135.3",
]

[tool.fastapi]
entrypoint = "src.philosophy.api.main:app"
